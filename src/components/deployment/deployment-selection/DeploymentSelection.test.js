// note: most of unit testing logic has been moved to the dpeloyment selection list

describe('DeploymentSelection', () => {
  it('should have more testing around it', () => {
    expect(true).toEqual(true);
  })
});
